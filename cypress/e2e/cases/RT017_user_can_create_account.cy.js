describe('RT017_user_can_create_account', () => {

  let shared_data = {};

  before(() => {
    cy.npmAutoLinkInit();
  })

  // XYZ.....
  it('XYZ.....', () => {
    cy.session(
      'xyz_',
      () => {
        // TODO....
      }
    );
  })

})
